# Copiar a .env.local para desarrollo o definir en el build de producción

# API (opcional en dev; Vite proxy usa localhost:3000 por defecto)
# VITE_API_URL=http://localhost:3000/api

# CloudFront — mismas fotos que en producción (sin barra final)
VITE_CDN_URL=https://d18o1scxevdeid.cloudfront.net
VITE_S3_BUCKET=chambing
VITE_AWS_REGION=eu-north-1
